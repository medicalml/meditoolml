Usage:

1. Create/modify preprocessing method to divide data into ID's and inputs for network-
2. Use tensorflow.estimator.Estimator() object as classifier, or modify predict method for prefered framework

Usage for pickled data:

filenames = ["test_part_"+str(num)+".pickle" for num in [0,1,2,3,4,5,6,7,9]]
submis.create_submission(classifier,filenames,["id","is_iceberg"],"id")
